http:
  addr: ":80"
cursor:
  secret: "changeme"
jobs:
  universe_index:
    enabled: true
    schedule: "0 3 * * *"  # 3:00 AM daily in production
  translate_missing:
    enabled: true
    schedule: "0 */2 * * *"  # Every 2 hours in production
    batch_size: 200
