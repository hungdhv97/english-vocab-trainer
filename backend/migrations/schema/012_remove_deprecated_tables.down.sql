-- Down migration: Recreate deprecated tables (if needed)

-- Note: These tables are not recreated as they are deprecated
-- If rollback is needed, the tables should be recreated from their original schema
-- This is intentionally left empty as the tables should not be recreated

